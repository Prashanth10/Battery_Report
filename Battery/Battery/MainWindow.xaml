<Window
    x:Class="Battery.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Battery"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="startButton" Grid.Row="0" Grid.Column="0" Click="StartButton_Click">Start</Button>
            <Button x:Name="stopButton" Grid.Row="0" Grid.Column="0" Click="StopButton_Click">Stop</Button>
            
        </StackPanel>

        <StackPanel Grid.Row="1"  Margin="1,1,1,1" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Name="result" TextWrapping="Wrap">Results</TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="1,1,1,1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <TextBlock Name="Debug" TextWrapping="Wrap">Debug</TextBlock>
        </StackPanel>
    </Grid>

</Window>
